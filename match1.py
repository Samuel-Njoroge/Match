# A sequence of characters that define a search pattern .
# Can be used in a string-searching algorithm to find and replace.

# A  pattern that takes HTML code and removes comments
import re
    
print(re.match('meow', 'hormon'))